[pytest]
testpaths = tests
markers =
    slow: live external calls (Ollama, Facebook, etc); skipped by default
    unit: pure logic, no IO
    integration: hits IO (network, disk, subprocess)
addopts = -m "not slow"
timeout = 900
filterwarnings =
    ignore::DeprecationWarning
